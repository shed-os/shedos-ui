// shedos-greeter — greetd greeter that mirrors the hyprlock visual.
// Scaffold only at this point; the wlr-layer-shell surface, font
// rendering, password handling, and greetd IPC land in subsequent
// commits.

fn main() {
    eprintln!("shedos-greeter: scaffold build, no UI wired yet");
    std::process::exit(0);
}
